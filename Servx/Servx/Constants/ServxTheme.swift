//
//  ServxTheme.swift
//  Servx
//
//  Created by Makhlug Jafarov on 2025. 01. 11..
//

import Foundation
import SwiftUI

struct ServxTheme {
    static let primaryColor = Color("primary500")       // Primary color for headings and buttons
    static let secondaryTextColor = Color("primary300") // Secondary text color for input fields
    static let inputFieldBorderColor = Color("primary100") // Border color for input fields
    static let buttonDisabledColor = Color("disabledButton") // Disabled state for buttons
    static let linkTextColor = Color("primary400")      // Link and active button text color
    static let backgroundColor = Color.white           // Background color for components
    static let blackColor = Color.black                // Black color for general use
    static let greyScale400Color = Color("greyscale400")
    static let greyScale100Color = Color("greyscale100")
    
}
